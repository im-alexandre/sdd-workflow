interface:
  display_name: "SDD Implement Feature"
  description: "Single-command SDD skill for strict implementation"
  short_description: "Implement from frozen MANIFEST"
  default_prompt: "Use /implement_feature to execute the frozen MANIFEST with approvals."

agent:
  name: sdd-implement-feature
  description: "Execute an approved implementation plan strictly inside the frozen MANIFEST, with diff approvals, verification, and no scope expansion."

