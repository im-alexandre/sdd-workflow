interface:
  display_name: "SDD Plan Feature"
  description: "Single-command SDD skill for executable planning"
  short_description: "Freeze MANIFEST plan"
  default_prompt: "Use /plan_feature to generate a frozen executable plan."

agent:
  name: sdd-plan-feature
  description: "Generate a read-only executable feature plan, freeze the MANIFEST, and update Codex planning artifacts without modifying source code."

